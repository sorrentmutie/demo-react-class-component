import { Component, ReactNode } from "react";

export class Esempio extends Component {
    render(): ReactNode {
        return (
            <div>
                <h3>Esempio</h3>
            </div>
        );
    }
}